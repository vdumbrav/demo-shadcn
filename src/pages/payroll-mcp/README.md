# Страница Payroll MCP

Комплексный интерфейс управления зарплатой, построенный с использованием модульных компонентов, организованных по уровню сложности. Эта страница демонстрирует полный панель управления со статистикой, графиками, таблицами и интерактивными диалогами.

## 📁 Структура проекта

```
payroll-mcp/
├── components/                  # Компоненты страницы
│   ├── index.ts                # Barrel export всех компонентов
│   ├── AdvancedFilterDialog.tsx # 🔴 Расширенная фильтрация
│   │
│   ├── StatsCards.tsx          # 🟢 Простой компонент
│   │
│   ├── VisitorsChart.tsx       # 🟡 Средняя сложность
│   ├── PayrollSidebar.tsx      # 🟡 Средняя сложность
│   │
│   ├── PayrollTable.tsx        # 🔴 Сложный компонент
│   ├── StartPayrollDialog.tsx  # 🔴 Сложный компонент
│   ├── EditPaymentDialog.tsx   # 🔴 Сложный компонент
│   ├── DeletePaymentDialog.tsx # 🔴 Сложный компонент
│   └── BatchDeleteDialog.tsx   # 🔴 Сложный компонент
│
└── data/                        # Mock данные и утилиты
    ├── mockData.ts             # Все тестовые данные и типы
    ├── validationSchemas.ts    # Zod валидационные схемы
    ├── dragDropUtils.ts        # Утилиты drag-and-drop
    ├── exportUtils.ts          # Утилиты экспорта (CSV, JSON, PDF)
    └── filterUtils.ts          # Утилиты фильтрации
```

## 📦 Обзор компонентов

### 🟢 Простые компоненты

#### `StatsCards.tsx`

**Назначение**: Отображение 4 карточек со статистикой и метриками

**Сложность**: Низкая
**Состояние**: Без состояния (нет локального state)
**Источник данных**: `statsData` из mockData

**Функции**:

- Адаптивный макет сетки (1 кол → 2 кол → 4 кол)
- Индикаторы значков с иконками тренда вверх/вниз
- Отображает: название, значение, процентное изменение, описание тренда
- Использует иконки lucide-react (TrendingUp, TrendingDown)

**UI компоненты**: Card, CardHeader, Badge

---

### 🟡 Средняя сложность

#### `VisitorsChart.tsx`

**Назначение**: Интерактивный график статистики посетителей в разные периоды времени

**Сложность**: Средняя
**Состояние**: `activePeriod` (today | last-7-days | last-30-days | last-3-months | last-year)
**Источник данных**: `chartDataByPeriod`, `chartConfig` из mockData

**Функции**:

- Адаптивный график площадей с заливкой градиента
- Группа переключения для выбора периода (5 опций)
- **Оптимизация для мобильных**: только иконки на мобильных, текст + иконка на настольных
- Динамические данные графика в зависимости от выбранного периода
- Плавный график площадей с подсказками

**UI компоненты**: Card, ToggleGroup, ChartContainer, Recharts

---

#### `PayrollSidebar.tsx`

**Назначение**: Боковая панель навигации для приложения Payroll MCP

**Сложность**: Средняя
**Состояние**: Без состояния
**Функции**: Навигация, меню платформы, список проектов, информация пользователя

**UI компоненты**: Sidebar, Avatar, Navigation items

---

### 🔴 Сложные компоненты

#### `PayrollTable.tsx`

**Назначение**: Продвинутая таблица данных с фильтрацией, сортировкой, постраничной разбивкой и действиями

**Сложность**: Высокая

**Управление состоянием**:

- `activeTab`: Текущая вкладка фильтра (4 категории)
- `currentPage`: Текущая страница
- `rowsPerPage`: Количество строк на странице
- `sortColumn`: Сортируемый столбец
- `sortDirection`: Направление сортировки (asc | desc)
- `visibleColumns`: Видимость столбцов
- `checkedItems`: Выбранные строки
- `dragState`: Состояние drag-and-drop

**Функции**:

- ✅ **Вкладки**: 4 фильтра категорий с подсчетом элементов
- ✅ **Drag & Drop**: Реордеринг строк с визуальной обратной связью
- ✅ **Сортировка**: Сортируемые столбцы
- ✅ **Выделение**: Individual и batch selection
- ✅ **Фильтрация**: Расширенные фильтры с AdvancedFilterDialog
- ✅ **Экспорт**: CSV, JSON, TSV, PDF
- ✅ **Пагинация**: Навигация по страницам
- ✅ **Мобильная оптимизация**: Адаптивный дизайн

**UI компоненты**: Table, Tabs, Dialog, DropdownMenu, Select, Badge

---

#### `AdvancedFilterDialog.tsx`

**Назначение**: Диалог для расширенной фильтрации данных таблицы

**Сложность**: Высокая

**Функции**:

- Фильтрация по статусу (Done | In Process)
- Фильтрация по типу раздела
- Фильтрация по диапазону значений (Target, Limit)
- Применение и сброс фильтров
- Отображение активных фильтров

**UI компоненты**: Dialog, Input, Select, Button, Badge

---

#### `StartPayrollDialog.tsx`

**Назначение**: Модальный диалог для создания нового цикла зарплаты

**Сложность**: Высокая

**Интеграции**:

- ✅ React Hook Form для управления формой
- ✅ Zod для валидации схемы
- ✅ DateRangePicker для выбора периода
- ✅ DatePicker для выбора даты платежа

**Функции**:

- Выбор типа зарплаты (новая или повторение)
- Ввод названия зарплаты
- Выбор периода (диапазон дат)
- Выбор даты платежа
- Опция повторения

---

#### `EditPaymentDialog.tsx`

**Назначение**: Модальный диалог для редактирования информации платежа сотрудника

**Сложность**: Высокая

**Интеграции**:

- ✅ React Hook Form для управления формой
- ✅ Zod для валидации полей
- ✅ Автоматическое сохранение данных

**Функции**:

- Выбор сотрудника
- Выбор способа выплаты (Крипто/Банк)
- Ввод кошелька/IBAN
- Выбор сети блокчейна
- Выбор токена валюты
- Ввод суммы платежа

---

#### `DeletePaymentDialog.tsx` & `BatchDeleteDialog.tsx`

**Назначение**: Диалоги подтверждения удаления платежей

**Сложность**: Средне-Высокая

**Функции**:

- Простое подтверждение для одиночного удаления
- Отображение количества элементов для массового удаления
- Деструктивное оформление

---

## 🛠️ Утилиты данных (data/)

### `validationSchemas.ts`

Zod схемы для валидации форм:

- `startPayrollSchema` - валидация создания зарплаты
- `editPaymentSchema` - валидация редактирования платежа

### `dragDropUtils.ts`

Утилиты для реордеринга строк:

- `handleDragStart`, `handleDragOver`, `handleDragLeave`
- `handleDrop`, `handleDragEnd`

### `exportUtils.ts`

Функции экспорта данных:

- `exportToCSV` - экспорт в CSV
- `exportToJSON` - экспорт в JSON
- `exportToTSV` - экспорт в TSV (Excel)
- `printAsPDF` - печать в PDF

### `filterUtils.ts`

Утилиты фильтрации:

- `applyFilters` - применение множественных фильтров
- `getDefaultFilters` - получение фильтров по умолчанию
- `FilterOptions` - типы фильтров

### `mockData.ts`

Тестовые данные:

- `tableData` - 68 строк тестовых данных таблицы
- `chartDataByPeriod` - данные для графиков по периодам
- `statsData` - данные для карточек статистики
- `availableReviewers` - список рецензентов
- `TableRowData` - типы для данных таблицы

---

## ✨ Ключевые особенности

### Валидация форм

- ✅ React Hook Form для управления состоянием форм
- ✅ Zod для валидации данных
- ✅ Real-time валидация с сообщениями об ошибках
- ✅ Типо-безопасные формы (TypeScript)

### Drag & Drop

- ✅ Реордеринг строк таблицы
- ✅ Визуальная обратная связь во время перетягивания
- ✅ Гладкие анимации
- ✅ Мобильно-дружелюбный

### Экспорт данных

- ✅ CSV экспорт для Excel
- ✅ JSON экспорт для программистов
- ✅ TSV экспорт для таблиц
- ✅ PDF печать для документов

### Фильтрация и сортировка

- ✅ Множественные критерии фильтрации
- ✅ Сортировка по любому столбцу
- ✅ Фильтры сохраняются в UI
- ✅ Сброс фильтров одной кнопкой

### Адаптивный дизайн

- ✅ Мобильный-первый подход
- ✅ Адаптивные гриды и кнопки
- ✅ Горизонтальная прокрутка на мобильных
- ✅ Touch-friendly взаимодействие

---

## 🚀 Использование

### Импорт компонентов

```typescript
import {
  StatsCards,
  VisitorsChart,
  PayrollSidebar,
  PayrollTable,
  StartPayrollDialog,
  EditPaymentDialog,
} from '@/pages/payroll-mcp/components';
```

### Пример макета страницы

```tsx
import { SidebarProvider } from '@/components/ui/sidebar';
import { PayrollSidebar, StatsCards, VisitorsChart, PayrollTable } from './payroll-mcp/components';

export default function PayrollMcpPage() {
  return (
    <SidebarProvider>
      <div className="flex min-h-screen">
        <PayrollSidebar />
        <main className="flex-1 space-y-6 p-6">
          <StatsCards />
          <VisitorsChart />
          <PayrollTable />
        </main>
      </div>
    </SidebarProvider>
  );
}
```

---

## 🛠️ Технологический стек

- **React 19** - UI фреймворк
- **TypeScript** - Type-safe разработка
- **shadcn/ui** - Компоненты UI
- **Tailwind CSS v4** - Стили
- **React Hook Form** - Управление формами
- **Zod** - Валидация данных
- **Recharts** - Графики
- **lucide-react** - Иконки

---

## 📝 Заметки разработки

### Организация компонентов

- **Простые**: Компоненты без состояния, презентационные
- **Средние**: Компоненты с локальным состоянием и умеренной логикой
- **Сложные**: Несколько переменных состояния, побочные эффекты, вложенные компоненты

### Качество кода

- ✅ Последовательные соглашения об именовании
- ✅ Модульные и переиспользуемые компоненты
- ✅ Четкое разделение ответственности
- ✅ Полные комментарии JSDoc
- ✅ ESLint + Prettier форматирование
- ✅ Strict TypeScript проверка

### Мобильная оптимизация

- ✅ VisitorsChart с адаптивными кнопками
- ✅ Таблица с горизонтальной прокруткой на мобильных
- ✅ Адаптивные формы и диалоги
- ✅ Touch-friendly размеры кнопок

---

## 🔮 Будущие улучшения

1. **API интеграция** - замена mock данных на реальные API
2. **Real-time обновления** - WebSocket для живых данных
3. **Тестирование** - Unit тесты с Jest/Vitest
4. **Storybook** - документация компонентов
5. **Интернационализация** - поддержка i18n
6. **Аналитика** - отслеживание событий пользователя

---

## 📄 Лицензия

Часть проекта demo-shadcn.
